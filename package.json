{
  "name": "slack-playground",
  "version": "2.0.0",
  "description": "Slack playground as a place for figuring out how to build the floating book club",
  "main": "app.js",
  "scripts": {
    "ngrok": "ngrok http 3000",
    "start": "node dist/app.js --enable-source-maps",
    "postinstall": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.0",
    "@types/pg": "^8.6.1",
    "typescript": "^4.2.0"
    },
  "dependencies": {
    "@slack/bolt": "^3.7.0",
    "pg": "^8.7.0"
  }
}
